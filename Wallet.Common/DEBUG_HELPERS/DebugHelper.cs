namespace Wallet.Common.DEBUG_HELPERS
{
    public class DebugHelper
    {
        public bool IsRemoved { get; set; }

        public void RemoveStoredValue(string key)
        {
            //todo:
            //remove value by key
            //validate removing
        }

        private void ValidateRemoving(string key)
        {
            //change flag `IsRemoved` depending to result od validation
        }
    }
}
